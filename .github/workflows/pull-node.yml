name: remote ssh command
on: 
  push:
    branches: [main]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands using password
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: '192.168.1.4'
        username: 'dradap'
        password: '@Dradap123'
        port: 22
        script: |
          cd /home/dradap/Documents/test2/LatihanServer
          git pull
          
